## Packages
(none needed)

## Notes
Using base stack which includes framer-motion, lucide-react, react-hook-form, zod, wouter, etc.
Assuming @shared/routes exports `api` and `buildUrl`.
Assuming @shared/schema exports `InsertResume`, `InsertCoverLetter`, `InsertInterviewPrep`.
